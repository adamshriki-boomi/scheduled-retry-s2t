# Quality Tests

components to consume in the app:

## `<QualityTestRenderer />`

- A conditional renderer according to block type

## `<QaulityTestsButton />`

- A modal button renderer - opens a modal with relevant tests
- expects to get a list of test oid's
- responsible for interacting with the tests api for CRUD operations
