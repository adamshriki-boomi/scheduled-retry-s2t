import { ComponentStyleConfig } from '@chakra-ui/theme';

const Link: ComponentStyleConfig = {
  baseStyle: {},
  sizes: {
    base: {
      px: '16px',
      py: '7.5px',
      height: '36px',
    },
  },
};

export default Link;
